import yfinance as yf

# symbol = input("")
# info = yf.Ticker(symbol).info
# price = yf.Ticker(symbol).info['price']
# sector = yf.Ticker(symbol).info['sector']
# peRatio = yf.Ticker(symbol).info['forwardPE']
# dividend = yf.Ticker(symbol).info['dividendRate']
# marketCap = yf.Ticker(symbol).info['marketCap']
# fiftyTwoWeekHigh = yf.Ticker(symbol).info['fiftyTwoWeekHigh']
# fiftyTwoWeeklow = yf.Ticker(symbol).info['fiftyTwoWeekLow']
# for stat in info:
#     print(stat, info[stat])
# quote_table = yf.get_quote_table()

